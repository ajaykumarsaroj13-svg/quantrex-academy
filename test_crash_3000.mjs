import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', err => console.log('PAGE ERROR:', err.toString())); await page.goto('http://localhost:3000/quantrex-academy/'); await new Promise(r => setTimeout(r, 2000)); await page.evaluate(() => { const tabs = Array.from(document.querySelectorAll('button')); const pyqTab = tabs.find(t => t.textContent.includes('PYQs')); if (pyqTab) pyqTab.click(); }); await new Promise(r => setTimeout(r, 1000)); await page.evaluate(() => { const tabs = Array.from(document.querySelectorAll('button')); const topicTab = tabs.find(t => t.textContent.includes('Topic Wise')); if (topicTab) topicTab.click(); }); await new Promise(r => setTimeout(r, 2000)); console.log('Done checking.'); await browser.close(); })();
