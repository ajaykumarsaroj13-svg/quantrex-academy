import mongoose from 'mongoose'; import dotenv from 'dotenv'; import { Pyq } from './backend/models/schemas.js'; dotenv.config(); mongoose.connect(process.env.MONGODB_URI).then(async () => { const qs = await Pyq.find({ chapterId: 'ch_mathematics_algebra_5' }); for(let q of qs){ q.topic = q.topic.split('-').map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(' '); await q.save(); } console.log('Done matrices topics formatting'); process.exit(0); });
