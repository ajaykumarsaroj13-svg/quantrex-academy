const fs = require('fs'); const scriptStr = fs.readFileSync('E:/QuantrexAcademy_Workspace/public/blackbook-script.js', 'utf8'); const fullData = JSON.parse(fs.readFileSync('E:/QuantrexAcademy_Workspace/blackBookDataFull.json', 'utf8')); const ch = fullData.find(c => c.id === 'indefinite-and-definite-integration'); let arr = JSON.parse(scriptStr.replace('window.DEFAULT_BLACKBOOK = ', '').replace(/;$/, '')); const idx = arr.findIndex(c => c.id === ch.id); if(idx !== -1) { arr[idx] = ch; } else { arr.push(ch); } fs.writeFileSync('E:/QuantrexAcademy_Workspace/public/blackbook-script.js', 'window.DEFAULT_BLACKBOOK = ' + JSON.stringify(arr, null, 2) + ';'); console.log('Chapter added successfully!');
